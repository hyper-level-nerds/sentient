

namespace example:

    @pack
    model example_model:
        number : u64
        email_address : str8[256]
        password : str8[65] $cred $encrypt(sha-256)
        health : f64
        created_timestamp : dt64
        updated_timestamp : dt64

    