#include <cstdlib>

#include "./program.hpp"

namespace sntc {

program::exit_code_t program::execute(int argc, char** argv)
{
    exit_code_t result = EXIT_FAILURE;

    do
    {

    }
    while (false);

    return result;
}

}