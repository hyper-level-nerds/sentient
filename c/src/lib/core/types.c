#include <sentient/core/types.h>

#include <sys/time.h>

bool sentient_scdt32_local_now(sentient_scdt32* dt)
{
    bool result = true;

    

    return result;
}

bool sentient_scdt64_local_now(sentient_scdt64* dt)
{
    bool result = true;

    return result;
}

bool sentient_pt64_local_now(sentient_pt64* pt)
{
    bool result = true;

    return result;
}

bool sentient_tv128_local_now(sentient_tv128* tv)
{
    bool result = true;

    return result;
}

bool sentient_scdt32_utc_now(sentient_scdt32* dt)
{
    bool result = true;

    return result;
}

bool sentient_scdt64_utc_now(sentient_scdt64* dt)
{
    bool result = true;

    return result;
}

bool sentient_pt64_utc_now(sentient_pt64* pt)
{
    bool result = true;

    return result;
}

bool sentient_tv128_utc_now(sentient_tv128* tv)
{
    bool result = true;

    return result;
}

bool sentient_get_tm_from_scdt32(struct tm* tm, sentient_scdt32* dt)
{
    bool result = true;

    return result;
}

bool sentient_get_scdt32_from_tm(sentient_scdt32* dt, struct tm* tm)
{
    bool result = true;

    return result;
}

bool sentient_get_tm_from_scdt64(struct tm* tm, sentient_scdt32* dt)
{
    bool result = true;

    return result;
}

bool sentient_get_scdt64_from_tm(sentient_scdt32* dt, struct tm* tm)
{
    bool result = true;

    return result;
}
